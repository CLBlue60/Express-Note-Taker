/*default styles*/
html,
body {
  height: 100%;
  margin: 0;
  padding: 0;
}

/* Hide save, new, and clear buttons by default */
.save-note,
.new-note,
.clear-btn {
  display: none;
}

/* Styling for icons */
.icons {
  margin-left: 15px;
  margin-right: 15px;
  font-size: 1.5rem;
  cursor: pointer;
}

/* Styling for note textarea and title */
.note-textarea,
.note-title {
  color: #000000;
  width: 100%;
  outline: none;
  border: none;
}

/* Styling for note textarea */
.note-textarea {
  resize: none;
  height: calc(100% - 63px);
  padding-top: 15px;
}

/* Styling for note title */
.note-title {
  height: 50px;
  margin-top: 13px;
  font-size: 3rem;
}

/* Styling for read-only mode */
.note-title:read-only,
.note-textarea:read-only {
  color: #757575;
}

/* Styling for list container */
.list-container .card {
  height: calc(100vh - 70px);
  border-bottom: none;
  border-bottom-right-radius: 0;
  border-bottom-left-radius: 0;
}

.list-container {
  padding-left: 0;
}

.list-container .list-group,
.list-container .list-group-item:first-child,
.list-container .card {
  border-top: none;
  border-top-right-radius: 0;
  border-top-left-radius: 0;
}

/* Styling for list item title */
.list-item-title {
  display: inline-block;
  width: 75%;
}

/* Styling for delete icon */
.list-container i {
  font-size: 1.1rem;
  cursor: pointer;
}

/* Styling for list group item */
.list-container .list-group-item {
  border-left: none;
  border-right: none;
  padding: 20px;
  cursor: pointer;
}
